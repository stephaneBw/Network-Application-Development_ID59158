<Window x:Class="ExchangeOffice.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exchange Office - Lab 8" Height="220" Width="420">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Currency code (e.g. USD, EUR):" />
        <TextBox x:Name="CurrencyTextBox" Grid.Row="1" Margin="0,8,0,0" Text="USD"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Get current rate" Width="140" Click="GetRateButton_Click"/>
            <TextBlock x:Name="RateTextBlock" Margin="16,4,0,0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>